import "@testing-library/jest-dom";
import { render } from "@testing-library/react";
import AddToCollection from ".";

test("renders the AddToCollection component", () => {
  render(<AddToCollection />);
});
